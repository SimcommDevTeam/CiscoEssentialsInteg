# Sample Audio

The recording grid uses an embedded static WAV data URI in `src/lib/audio.ts` so playback and downloads work in a fully static export without a backend.

Replace that value with files in this folder, such as `/audio/sample-call-1.wav`, when production recording samples are available.
